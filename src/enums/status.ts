export enum TaskStatus{
    Pending='pending',
    InProgress = 'InProgress',
    Completed = 'Completed'
}

export enum TaskImportance{
    High='high',
    Half = 'half',
    Low = 'low'
}

export enum ProjectStatus{
    Active='active',
    Inactive='inactive'
}

